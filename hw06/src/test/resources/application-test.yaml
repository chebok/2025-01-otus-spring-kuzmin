spring:
  main:
    banner-mode: off
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: data.sql
